export const metadata = {
  title: 'Create Your Raise Negotiation Playbook — SalaryPrep',
  description: 'Fill in your role details to generate a personalized raise negotiation playbook.',
  robots: { index: false, follow: false },
};

export default function Layout({ children }) {
  return children;
}
