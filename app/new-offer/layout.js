export const metadata = {
  title: 'Create Your Offer Negotiation Playbook — SalaryPrep',
  description: 'Fill in your offer details to generate a personalized negotiation playbook.',
  robots: { index: false, follow: false },
};

export default function Layout({ children }) {
  return children;
}
